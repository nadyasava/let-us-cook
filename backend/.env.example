# Copy this file to .env and fill in your key to enable real food detection.
# Without it, /api/detect returns a mocked result so the app still runs end to end.
ANTHROPIC_API_KEY=
VISION_MODEL=claude-sonnet-4-6
